{% extends "layouts/base.njk" %}

{% block content %}
  <section class="event-hero mb-4" style="
      background-image: url('/images/{{ page.data.image }}'); 
      height: 40vh; background-size: cover; background-position: center;">
    <div class="hero-overlay container text-white">
      <h1>{{ page.data.title }}</h1>
      <p>{{ page.data.date | readableDate }}{% if page.data.time %} · {{ page.data.time }}{% endif %}</p>
      <p>{{ page.data.location }}</p>
    </div>
  </section>

  <article class="container py-4">
    {{ content | safe }}

    {% if page.data.location %}
      <div class="map my-4">
        <iframe
          src="https://www.google.com/maps?q={{ page.data.location | urlEncode }}&output=embed"
          width="100%" height="300" style="border:0;" allowfullscreen=""
          loading="lazy">
        </iframe>
      </div>
    {% endif %}
  </article>
{% endblock %}
